import { MouseParallaxDirective } from './mouse-parallax.directive';

describe('MouseParallaxDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseParallaxDirective();
    expect(directive).toBeTruthy();
  });
});
